export namespace BackgroundUpdate {
    namespace REASON {
        let CANNOT_USUALLY_CHECK: string;
        let CANNOT_USUALLY_STAGE_AND_CANNOT_USUALLY_APPLY: string;
        let LANGPACK_INSTALLED: string;
        let MANUAL_UPDATE_ONLY: string;
        let NO_DEFAULT_PROFILE_EXISTS: string;
        let NOT_DEFAULT_PROFILE: string;
        let NO_APP_UPDATE_AUTO: string;
        let NO_APP_UPDATE_BACKGROUND_ENABLED: string;
        let NO_MOZ_BACKGROUNDTASKS: string;
        let NO_OMNIJAR: string;
        let SERVICE_REGISTRY_KEY_MISSING: string;
        let UNSUPPORTED_OS: string;
        let WINDOWS_CANNOT_USUALLY_USE_BITS: string;
        let APPBASEDIR_NOT_WRITABLE: string;
    }
    let EXIT_CODE: any;
}

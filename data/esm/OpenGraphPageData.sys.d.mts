export namespace OpenGraphPageData {
    /**
     * Collects the opengraph data from the page.
     *
     * @param {Document} document
     *   The document to collect from
     *
     * @returns {PageData}
     */
    function collect(document: Document): PageData;
}
